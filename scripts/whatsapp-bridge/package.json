{
  "name": "hermes-whatsapp-bridge",
  "version": "1.0.0",
  "description": "WhatsApp bridge for Hermes Agent using Baileys",
  "private": true,
  "type": "module",
  "scripts": {
    "start": "node bridge.js",
    "check": "node --check allowlist.js && node --check baileys_logger.js && node --check attach.js && node --check bridge.js && node --check bridge_helpers.js && node --check connection_close.js && node --check outbound_ids.js && node --check owner_message_gate.js && node --test"
  },
  "dependencies": {
    "@whiskeysockets/baileys": "7.0.0-rc13",
    "express": "^4.21.0",
    "qrcode-terminal": "^0.12.0",
    "pino": "^9.0.0"
  },
  "overrides": {
    "protobufjs": "^7.5.5",
    "body-parser": "1.20.6"
  }
}
